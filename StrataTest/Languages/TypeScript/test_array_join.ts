let arr_to_join: string[] = ["Hello", "world", "from", "Strata"];
let joined_str1: string = arr_to_join.join();
let joined_str2: string = arr_to_join.join(" ");

let number_arr: number[] = [1, 2, 3, 4, 5];
let joined_numbers: string = number_arr.join();
let joined_numbers_with_dash: string = number_arr.join("-");
