// Test functions with multiple parameters
function calculate(a: number, b: number, c: number): number {
    let temp: number = a + b;
    return temp * c;
}

let result: number = calculate(2, 3, 4);
let final: number = result + 10;
