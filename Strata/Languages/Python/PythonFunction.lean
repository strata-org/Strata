import Strata.DL.Heap.Heap

---------------------------------------------------------------------

namespace PythonStrata

/-! ## Python Function Definitions

Re-export the generic Strata function types for Python compatibility.
The actual function definitions are now part of the core Strata Heap dialect.
-/

-- Re-export the generic types with Python-friendly names
--abbrev PythonFunction := Heap.HeapStrataFunction

end PythonStrata
