import Strata.DL.Heap.Heap

---------------------------------------------------------------------

namespace TSStrata

/-! ## TypeScript Function Definitions

Re-export the generic Strata function types for TypeScript compatibility.
The actual function definitions are now part of the core Strata Heap dialect.
-/

-- TODO: Restore when we figure out function architecture
-- Re-export the generic types with TypeScript-friendly names
-- abbrev TSFunction := Heap.HeapStrataFunction

end TSStrata
