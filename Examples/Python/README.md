# Python to Laurel examples

This folder contains examples of concrete Laurel after translation from Python. Note that `<test>.laurel.st` files are LLM generated extractions from the full translations `<test>.full.laurel.st`

The translations come from concrete examples `<test>.py`. We keep, for reproducibility's sake, the artifact files `<test>.python.ion` 
