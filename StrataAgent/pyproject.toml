[project]
name = "strataagent"
version = "0.1.0"
description = "Multi-agent orchestration framework for claude_agent_sdk"
requires-python = ">=3.14"
dependencies = [
    "claude-agent-sdk>=0.2.82",
    "jinja2>=3.1",
    "pydantic>=2.0",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
]
