name: LeanSwarm
manager: TaskManager
nudge_module: null
checkpoint:
  enabled: true
  interval_minutes: 5

agents:
  - search_agent
  - task_manager
  - proof_ledger
  # All other agents are created dynamically:
  # - counter_example, prover: by TaskManager via swarm_agent()
  # - po_decomposer, po_assessor, proof_writer, deep_proof_validator: by Prover via swarm_agent()
  # - tm_clarifier, tm_monitor, tm_chat: by TaskManager internally
