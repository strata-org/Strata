name: LeanSwarm
manager: TaskManager
nudge_module: null
checkpoint:
  enabled: true
  interval_minutes: 5

agents:
  - search_agent
  - task_manager
  # All other agents are created dynamically by the PO workflow:
  # - proof_writer_v2, proof_guide, goal_analyzer, lemma_extractor: by Prover (po_v3)
  # - counter_example: by Prover for CEA check
  # - tm_clarifier, tm_monitor: by TaskManager internally
